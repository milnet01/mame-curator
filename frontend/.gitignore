# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# `dist/` ignored during P06 development; the line above is removed at P06 step 18
# when the production bundle gets committed (see root `.gitignore` for the long
# explanation of why frontend/dist/ ships).

*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
