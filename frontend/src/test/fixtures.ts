/**
 * Shared test fixtures.
 *
 * Per-component tests import the slices they need. The 3,000-card grid
 * fixture (used by LibraryGrid virtualization tests) is built lazily so
 * tests that don't need it don't pay the construction cost.
 */
