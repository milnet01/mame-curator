# P10 — Media coverage expansion — journal

Phase item: `mame-curator-1005` (Kind: implement; lanes media / frontend /
tests). Long-form spec + per-chunk implementation notes:
[`docs/specs/P10.md`](../specs/P10.md).

## Implementation (chunks 1–11)

Shipped tests-first, one chunk per commit, direct-push to `main`, each with a
`/cold-eyes` loop on its spec amendment before implementation:

- **1–5** foundations + LibretroSource move + progettoSnaps (CLI + file://
  source) + ArcadeDB + Wikipedia-image.
- **6** MobyGames key-handling half (cover-fetch deferred → `mame-curator-1079`).
- **7** `MediaSourceRegistry` + `resolve_image` orchestrator + `build_registry`
  + route wiring; the media 502 error surface retired for the fallback chain.
- **8** `resolve_wikipedia_extract` + `GET /media/{name}/wiki`.
- **9** readiness API (`GET /api/media/sources`) + key-paste route
  (`PUT …/secret`, loopback-trust) + `build_all_sources`.
- **10** Settings → Media source list (reorder + status + Configure/Download
  modals). Enable/disable checkbox deferred → `mame-curator-1084`.
- **11** Wikipedia "About" paragraph in the Alternatives drawer.

All chunks green on Linux/macOS/Windows CI.

## Close attempt — 2026-07-01 (BLOCKED → FP32)

`/close-phase` ran `/audit` (0 errors; 29 warnings were all the scrubbed-env
`mypy` "cannot find fastapi/yaml" false positive — CI mypy is clean —
recorded as `allowlist-015`) + a 3-lane `/indie-review`. The review surfaced
real defects the spec-level cold-eyes loops could not catch (they reviewed
contracts, not code paths): a non-object-JSON response that escapes
parse-before-trust and 500s + poisons the cache (H1), a clipboard copy that
reports false success (H2), a progettoSnaps crash on a non-directory snap_dir
(M1), an unvalidated Wikipedia URL href (M2), plus several LOW doc-drift /
dead-code / defensive items.

Per close-phase 5a (actionable findings > 0), **all findings were folded into
`FP32` [`mame-curator-1085`]** and the phase remains 🚧. Convergence checkpoint:
1 fix-pass in a row (well under 5). P10 closes cleanly once FP32 is worked
(TDD) and `/close-phase` re-runs with zero actionable findings.

### False positives confirmed this close

- `allowlist-015` — `/audit` `mypy` "cannot find fastapi / yaml" across the
  `api/` tree: the audit runs `mypy` in a scrubbed env without the project's
  installed deps, so every `fastapi`/`yaml` import flags; the project's own
  `uv run mypy` (deps installed) is clean in CI.

## Close attempt — 2026-07-02 (BLOCKED → FP33)

Re-ran `/close-phase` after FP32 shipped. `/audit` clean (28 warnings, all
`allowlist-015`). The 3-lane `/indie-review` confirmed FP32's four fixes
correct, but a deeper read of the source chain surfaced **two HIGH defects
FP32's first pass left open** — both the same *class* of bug FP32 targeted, one
level deeper:

- **H1 (SECURITY/LFI):** `resolve_image`'s `file://` short-circuit trusts a URL
  from *any* source, not just the local ProgettoSnaps pack. ArcadeDB's plaintext
  first hop + unvalidated upstream URL strings mean a MITM `file:///etc/passwd`
  is served as an image, bypassing the cache-layer scheme guard.
- **H2:** FP32's `isinstance(data, dict)` guard only covered the *top level*;
  four *nested* upstream accesses (`result[0]`, non-str URL fields, non-dict
  `thumbnail`, non-dict `content_urls.desktop`) still crash → route 500,
  including the "never-500s" wiki path.

Plus a residual M1 (progettoSnaps still crashes on a permission-denied dir) and
a cluster of LOW/INFO drift (three shipped media routes + `MediaSourceUnknownError`
absent from `api/spec.md`; a `DownloadPackModal` "Copied!" that doesn't reset on
reopen; a stale `check_error_codes_sync.py` comment for a gate that was never
built; `SourceSecret` verbatim-store; secret-route name coupling).

Per close-phase 5a, **all folded into `FP33` [`mame-curator-1086`]**; P10 stays
🚧. Convergence checkpoint: 2 fix-passes in a row (under 5). Audit again clean
modulo `allowlist-015` (no new allowlist entries this close).

## Close attempt — 2026-07-02 (CLOSED, tag `P10-complete`)

Re-ran `/close-phase` after FP33 shipped (3rd attempt). `/audit` clean (all
mypy `allowlist-015`, now also written to the machine-readable
`.ants_review_falsepos.jsonl` ledger via `audit_falsepos_log`). A 2-lane
verification review confirmed **every FP32 + FP33 fix correct and complete**;
its adversarial sweep found three tail findings (one real one-line robustness
guard + two doc-drift nits) → folded into **`FP34` [`mame-curator-1087`]**,
shipped same day. A 3rd review round was deliberately not run (severity had
trailed to one-liners; the user elected fix + targeted-verify + close).

**Convergence:** 3 fix-passes in a row (FP32/33/34), under the checkpoint of 5;
severity dropped monotonically (2 HIGH+2 MED → 2 HIGH+1 MED → 0 HIGH). The
`file://` LFI (FP33 H1) was the highest-value catch — a spec-level cold-eyes
loop could not have found it (it reviewed contracts, not the plaintext-hop code
path).

**Deferred to their own tracked items:** the co-located
`src/mame_curator/media/spec.md` (`mame-curator-1058`, now unblocked — the
module's contract still lives in `docs/specs/P10.md`); MobyGames cover-URL
fetch (`1079`); Settings enable/disable (`1084`); `media.snaps_dir` binding
(`1081`); Starlette httpx deprecation (`1082`).

### False positives confirmed this close

- `allowlist-015` re-confirmed (mypy scrubbed-env fastapi/yaml) and promoted to
  the `.ants_review_falsepos.jsonl` ledger so future sweeps pre-discard it
  without re-triage.
