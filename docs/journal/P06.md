# P06 — Frontend MVP

## Status

🚧 In-flight — closing fix-pass **FP11** active.

## What shipped (impl-steps 1–18)

- **1–5** scaffold + Tailwind v4 + 6 themes + 16 shadcn primitives + runtime/dev deps + Vitest/MSW/Playwright configs.
- **6–7** `frontend/src/api/types.ts` (57 hand-mirrored interfaces + zod schemas) + `client.ts` (typed `ApiError` envelope) + `tools/check_api_types_sync.py` stdlib-only Python ↔ TS drift CI gate.
- **8–9** `strings.ts` user-facing string catalogue + backend `_SPAStaticFiles` mount with deep-link fallback + 2 backend tests.
- **10** 19 components/pages under TDD (per spec § "Tests to write first") + the no-checkbox-for-prefs invariant test.
- **11–13** empty states embedded per component; `useKeyboard` hook (single-key + chord + meta-mode); `ErrorBoundary` class component with reset-on-key.
- **14–15** `AppShell` + `ThemeProvider` + `LibraryPage` container; `App.tsx` wires `QueryClientProvider` + `BrowserRouter` + lazy routes; Inter-fallback font + dark default + ≤300 ms motion budget in `index.css`.
- **16–18** production build (152 KB gzipped entry); Playwright E2E smoke + fixture config.yaml; `frontend/dist/` committed (`e569214`) with pre-commit hook excludes for minified output.

## Closing /audit + /indie-review (2026-05-02)

`/audit` summary across the 6 tools probed:

| Tool | Result |
|---|---|
| `npx eslint` (frontend) | 6 actionable + 2 allowlisted (vendored / library-by-design) |
| `npx tsc --noEmit` (frontend) | clean |
| `gitleaks detect` | 0 findings |
| `trivy fs --severity HIGH,CRITICAL` | 0 vulns / 0 secrets across `uv.lock` + `frontend/package-lock.json` |
| `semgrep p/security-audit + p/python + p/typescript + p/react + p/fastapi` | 1 finding (HelpPage `dangerouslySetInnerHTML` — corroborates indie-review L4) |
| `tools/check_api_types_sync.py` | clean (57 TS interfaces match 84 Pydantic models) |

`/indie-review` across **6 lanes** (api-bridge + library-components + alternatives-trio + pages + layout-+-hooks-+-app + backend-static-mount + test-infra/E2E/themes) surfaced **~40 actionable findings**.

Cross-cutting themes (≥ 2 lanes):

- **CC1** Inlined English strings outside `strings.ts` (4 lanes).
- **CC2** `App.tsx` non-Library routes wired with no-op stubs (2 lanes).
- **CC3** Loading/error states absent on async UI (2 lanes).
- **CC4** `_INTERFACE_RE` regex truncates at first `}` (2 lanes — both surfaced the same drift-gate hole).

## Close attempted; ~40 actionable findings folded into FP11

Phase remains 🚧 until **FP11** closes. See `ROADMAP.md` § FP11 for the full clustered bullet list (10 clusters: A–J).

## False positives confirmed

- **allowlist-002** — eslint `react-hooks/incompatible-library` on `useVirtualizer` (`@tanstack/react-virtual` library-by-design).
- **allowlist-003** — eslint `react-refresh/only-export-components` on shadcn-generated `components/ui/button.tsx` + `tabs.tsx` (vendored, not authored).

## What was deferred

None to `docs/known-issues.md` at this fold-in — every finding maps cleanly to a code or spec change inside FP11's scope. The full Playwright library-flow scenario (search → drawer → override → dry-run → copy → pause/resume → conflict resolve) is a known limitation of the current smoke E2E (P06.17 docblock acknowledges it); FP11 § B / D / H clusters cover the spec-conformance work that would let the full E2E land in a follow-up.
