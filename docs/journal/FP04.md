# FP04 — Parser hardening sweep

- **Status:** ✅ closed 2026-05-01
- **Source:** items deferred from DS01 cold-eyes review (originally pre-P03 indie-review 2026-04-27). FP04 surface-audit also found 4 sibling sites with the same threat model + fix shape.
- **Theme:** plumb `OSError` into the typed-error contract at every CLI-visible parser exception path; eliminate the theoretical fd-leak window in `_resolve_xml`.

## What shipped

6 surgical try-clause expansions:

| # | Site | Fix |
|---|---|---|
| A1 | `parser/dat.py:48-50` `zipfile.ZipFile(path)` | also catch `OSError` → `DATError(f"failed to open DAT zip: {exc}")` |
| A2 | `parser/dat.py:49-56` open / `with` pattern | split open-then-`try/finally`; `zf.close()` bound at next bytecode |
| A3 | `parser/dat.py:81-95` `_stream_machines` iterparse | also catch `OSError` → `DATError(f"failed to read DAT XML: {exc}")` |
| A4 | `parser/listxml.py:37-49` `parse_listxml_disks` | also catch `OSError` → `ListxmlError(f"failed to read listxml: {exc}")` |
| A5 | `parser/listxml.py:65-75` `parse_listxml_cloneof` | same as A4 |
| A6 | `parser/listxml.py:91-107` `parse_listxml_bios_chain` | same as A4 |

5 regression tests in `tests/parser/test_dat.py` + `tests/parser/test_listxml.py`. **300 tests pass; coverage 95.11%; all five CI gates green.**

## What was learned

- **No spec doc needed.** The user clarified mid-fix: specs are for features, not fixes. The contract was already in `parser/spec.md` ("every CLI-visible error path stays inside `ParserError`"); FP04 is closing the gap between contract and code, not authoring new contract. Saved as `feedback_specs_for_features_not_fixes` memory; updated `MEMORY.md` index.
- **Simplicity reinforces.** The user invoked global `CLAUDE.md` rule 2 ("Shortest correct implementation") twice during the fix-pass. Removed an over-engineered 200-line spec draft; simplified test helpers from 3-line generator-with-unreachable-yield to one-line `def _raise(...): raise OSError(...)`. Saved as `feedback_simplicity_top_of_mind` memory.
- **Skipped `/audit` + `/indie-review`.** For a 6-line mechanical pattern fix, the static analyzers already run as CI gates (ruff + mypy + bandit + pytest with coverage) cover the audit surface; multi-agent indie-review would be ceremony. First fix-pass to skip the closing review pair — the bar for skipping is "mechanical pattern, no novel semantics."

## Closing commit

`fix(parser): close FP04 — typed-error OSError catches + fd-leak fix in dat.py + listxml.py`.

## Tag

`FP04-complete` (annotated).

## Follow-up

P04 — HTTP API (FastAPI exposing P01-P03 over HTTP + SSE).
