# P04 — HTTP API (`api/`) — close attempted 2026-05-01

**Status:** still 🚧 — close attempted but indie-review surfaced 13 actionable findings, batched into FP09 per the App-Build "every audit finding is tracked" rule.

## What shipped (Step 4)

14 source files in `src/mame_curator/api/` (~2200 LoC), 11 test files in `tests/api/` (64 tests), `serve` CLI subcommand, `_atomic.atomic_write_bytes` helper (Rule of Three: third site after `executor.py` + `cli/`).

Surface:
- `app.py` — `create_app(config)` factory + lifespan that loads parsed world state.
- `errors.py` — typed `ApiException` hierarchy + global handlers.
- `schemas.py` — wire models for every route.
- `state.py` — frozen `WorldState` blob; `replace_world(...)` for atomic swaps.
- `persist.py` — atomic-write + snapshot/restore helpers.
- `fs.py` — sandbox allowlist composition + path validation + `os_drive_roots()`.
- `jobs.py` — singleton `JobManager` + `_ProgressSynthesizer` bridge from sync `run_copy(on_progress=...)` to async SSE event queue.
- `routes/{games,curate,config,copy,activity,fs,stubs,help,media}.py` — 40 routes (R01–R39 + R13b).

Tests pass: 363 across the project, 1 xfailed (P01 idempotency property test — by design pending PATCH-no-op identity work in FP09 B6). Coverage: 88.49% overall, ~86% on `api/`. All five gates green: pytest, ruff check, ruff format --check, mypy strict, bandit.

## Close attempted

`/audit` clean across all gates plus gitleaks, semgrep (p/security-audit + p/python + p/fastapi), and 9 project-specific grep gates. `/indie-review` returned 13 actionable + 4 LOW notes. Folded into [`FP09`](../specs/) — see `ROADMAP.md` § FP09. Phase remains 🚧 until FP09 closes; `/close-phase` re-run on P04 then closes cleanly.

## Drift / lessons (preliminary — full journal at clean close)

- **P04 spec sign-off was the right call** — 5-round cold-eyes review caught 23+ findings that would have been Step-4 rework otherwise.
- **Step-3 test bugs surfaced during Step-4 implementation:** sync `TestClient.get(/api/copy/status)` blocks on SSE; `httpx.ASGITransport` doesn't auto-run lifespan; raw `\n` in URL rejected by httpx pre-server. Fixed inline with documented comments.
- **Mini DAT fixture's listxml** (reused from `copy/`) didn't include `pacman/pacmanf` cloneof, so `/api/games/pacman/explanation` returned an empty hits list. Created `tests/api/fixtures/api_listxml.xml` with the missing cloneof + bios entries.
- **Windows CI fix bundled here** — `tests/parser/test_cli_parse.py::test_error_routes_to_stderr_with_path_prefix` was failing on Windows because `Path.__repr__` normalizes to POSIX slashes; assertion now accepts either native or `as_posix()` form.
