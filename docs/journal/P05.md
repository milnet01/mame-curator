# P05 — Media subsystem (`media/`) — in flight

## 2026-05-02 — close attempted

Close attempted; 5 actionable findings folded into FP10; phase remains 🚧 until FP10 closes.

Closing `/audit` clean across ruff (0) / bandit (0) / gitleaks (0).

Closing `/indie-review` against the cold-eyes brief surfaced 5 actionable findings (1 Tier 1, 1 Tier 2, 3 Tier 3) — see ROADMAP.md § FP10 for the full list. No false positives confirmed; no dependency-blocked items.

Implementation landmarks (commits `630a1f6` spec + `44e33ef` impl):

- `src/mame_curator/media/{__init__,urls,cache}.py` (~120 LoC) — escape rules, URL builder, sha256 lazy-fetch cache, typed exceptions.
- `src/mame_curator/api/routes/media.py` — R39 swapped to use the new helpers; `kind=video` load-bearing short-circuit.
- `src/mame_curator/api/app.py:43` — `AsyncClient(timeout=10.0)` (was bare).
- `tests/media/` (3 files, 43 tests) + extended `tests/api/test_routes_media.py` (4 new tests) + `tests/api/conftest.py` `media_cache_dir` fixture for tmp_path-isolated per-test cache.
- 419 tests pass; project 89.11%; `media/` 100%; all five gates green.
