# FP12 — Settings page list editors + path picker (in progress)

## Status

🚧 implementation complete (clusters A→J shipped 2026-05-02..04
across ~13 commits); FP12 stays 🚧 until FP13 closes.

Close attempted 2026-05-04 via `/close-phase`; closing `/audit`
clean but `/indie-review` surfaced **22 actionable findings**
folded into FP13. Final journal — what changed, what was learned,
deferrals — written when FP13 closes and FP12 is tagged at the
same SHA per the P05+FP10 / P06+FP11 precedent.

## Closing-audit summary (FP13 generated 2026-05-04)

- **Static analysis (clean across FP12 surface).** ruff ✅ /
  ruff format ✅ / mypy ✅ / bandit 0 issues / eslint ✅ /
  gitleaks 0 findings / semgrep 1 finding (on `HelpPage.tsx:67`
  — out of FP12 scope, P06/FP11 surface, deferred to next
  debt-sweep, not allowlisted).
- **3-lane indie-review (22 actionable).** Lanes: primitives
  (clusters A-E + I + J), FsBrowser + useFs (G), SettingsPage
  wiring (F + H + container + hook additions).
- **Cross-cutting theme** flagged by 2 of 3 lanes: **silent
  react-query mutation errors** — `useFsGrantRoot`,
  `useConfigPatch`, `useSnapshotRestore` all fire-and-forget
  without `onError`, so a 422 / 404 / 500 from the API is
  invisible to the user. Folded into FP13 § A.
- **Largest individual finding:** `SettingsPage.tsx` is **551
  lines** vs `docs/standards/coding-standards.md §2` hard-cap
  350. Extracts to per-tab subcomponents in FP13 § E1.
- **No allowlist additions** — every actionable finding is a
  real bug, regression, or standards violation. The one
  semgrep hit on HelpPage is real but out-of-scope for FP12.

See `ROADMAP.md § FP13` for the full 5-cluster breakdown.
