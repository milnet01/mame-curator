# <PREFIX>-NNNN — <topic> — build plan

**Spec:** [docs/specs/<ID>-<topic>.md](../specs/<ID>-<topic>.md)
**Status:** not started | in progress | done (YYYY-MM-DD).
<!-- A plan's own vocabulary — not the spec Status lifecycle. -->

<!-- A plan holds build steps only. Design rationale lives in the spec —
     documentation.md §10 owns that rule and says why. -->

## Steps

<Every step needs an observable verification. A step whose completion
 cannot be observed is a step nobody can tell you finished.
 Cite the invariant a step satisfies where it is not obvious.>

1. <action> → **verify:** <the observable check that it worked>
2. <action> → **verify:** <…> (satisfies INV-2)
3. <action> → **verify:** <…>

## Definition of done

<The whole-item check: the command you run, and what it prints when this
 is finished.>
